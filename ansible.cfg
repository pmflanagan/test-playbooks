[defaults]
deprecation_warnings=false

# Fact caching configuration for AAP
# Note: AAP has its own internal fact caching when use_fact_cache=True
# However, this ensures compatibility with standard Ansible fact caching
fact_caching = jsonfile
fact_caching_connection = /tmp/ansible_facts
fact_caching_timeout = 86400
